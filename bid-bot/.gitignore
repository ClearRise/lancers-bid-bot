# Local overrides (root .gitignore also applies when working from repo root)
node_modules/
.env
.env.*
!.env.example

# Playwright login session (any name pattern)
storage-state*.json
cookies.json

# Runtime state
bid-history.json
bid-queue.json

# Local prompts and filter settings
filter_settings/

# Build
dist/
*.tsbuildinfo

# Playwright artifacts
test-results/
playwright-report/
