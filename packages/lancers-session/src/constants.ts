/** Header “ログイン” link when session is not authenticated (Lancers). */
export const LANCERS_HEADER_LOGIN_HREF = "/user/login?ref=header_menu";

export const LANCERS_SESSION_PROBE_URL = "https://www.lancers.jp/";

export const LOGGED_OUT_DESKTOP_MESSAGE = "Logged out";

/** Default interval between session checks (10 minutes). */
export const DEFAULT_SESSION_STATUS_INTERVAL_MS = 600_000;
